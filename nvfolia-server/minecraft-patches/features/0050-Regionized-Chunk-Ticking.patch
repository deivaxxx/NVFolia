From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: NONPLAYT <nonplay@bxteam.org>
Date: Mon, 9 Jun 2025 13:51:43 +0300
Subject: [PATCH] Regionized Chunk Ticking

This patch adds regionized chunk ticking feature, by grouping adjacent chunks into regions and processing each region on its own thread.

Original idea by Dueris, modified by NONPLAYT and heavily optimized by dan28000

diff --git a/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java b/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
index 328784ba08236553e49d62320b79f7222292412f..05ef9cfaa29e6f3f8ee5d600fc201e46bdfe1114 100644
--- a/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/level/entity/EntityLookup.java
@@ -429,6 +429,11 @@ public abstract class EntityLookup implements LevelEntityGetter<Entity> {
         Entity currentlyMapped = this.entityById.putIfAbsent((long)entity.getId(), entity);
         if (currentlyMapped != null) {
             LOGGER.warn("Entity id already exists: " + entity.getId() + ", mapped to " + currentlyMapped + ", can't add " + entity);
+            // DivineMC start - Regionized Chunk Ticking - discard duplicates
+            if (entity.getRemovalReason() == null) {
+                entity.discard(null);
+            }
+            // DivineMC end - Regionized Chunk Ticking - discard duplicates
             return false;
         }
 
@@ -437,6 +442,11 @@ public abstract class EntityLookup implements LevelEntityGetter<Entity> {
             // need to remove mapping for id
             this.entityById.remove((long)entity.getId(), entity);
             LOGGER.warn("Entity uuid already exists: " + entity.getUUID() + ", mapped to " + currentlyMapped + ", can't add " + entity);
+            // DivineMC start - Regionized Chunk Ticking - discard duplicates
+            if (entity.getRemovalReason() == null) {
+                entity.discard(null);
+            }
+            // DivineMC end - Regionized Chunk Ticking - discard duplicates
             return false;
         }
 
diff --git a/net/minecraft/core/dispenser/DispenseItemBehavior.java b/net/minecraft/core/dispenser/DispenseItemBehavior.java
index c4e7c02c51f7732eeba46fcfc6047a63a7ffc0b2..6aec91364a2171a0ff3b5d4baaefe7ef1147da8a 100644
--- a/net/minecraft/core/dispenser/DispenseItemBehavior.java
+++ b/net/minecraft/core/dispenser/DispenseItemBehavior.java
@@ -293,7 +293,7 @@ public interface DispenseItemBehavior {
                 // Paper end - Call BlockDispenseEvent
                 List<org.bukkit.craftbukkit.block.CraftBlockState> capturedBlockStates; // Paper
                 org.bukkit.TreeType treeType; // Paper
-                level.captureTreeGeneration = true; // CraftBukkit
+                level.captureTreeGeneration.set(true); // CraftBukkit // DivineMC - Regionized Chunk Ticking - thread-safe block capture
                 try { // Paper
                 if (!BoneMealItem.growCrop(dispensed, level, target) && !BoneMealItem.growWaterPlant(dispensed, level, target, null)) {
                     this.setSuccess(false);
@@ -302,10 +302,10 @@ public interface DispenseItemBehavior {
                 }
                 // Paper start
                 } finally {
-                    level.captureTreeGeneration = false;
+                    level.captureTreeGeneration.set(false); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
 
-                    capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.values());
-                    level.capturedBlockStates.clear();
+                    capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.get().values()); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+                    level.capturedBlockStates.get().clear(); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
 
                     // DivineMC start - Parallel world ticking
                     treeType = net.minecraft.world.level.block.SaplingBlock.getTreeTypeTL();
diff --git a/net/minecraft/network/Connection.java b/net/minecraft/network/Connection.java
index 87dff5306c79b7f0fc940d9e85e523fa0c2f4ea6..80e46df1a18b66fff8abfc6d94b92a4767cac731 100644
--- a/net/minecraft/network/Connection.java
+++ b/net/minecraft/network/Connection.java
@@ -303,7 +303,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
 
     private static void syncAfterConfigurationChange(final ChannelFuture future) {
         try {
-            future.syncUninterruptibly();
+            future.awaitUninterruptibly(5000L); // DivineMC - In rare cases this can get stuck, so we time out instead in worst case 5s of lag
         } catch (Exception e) {
             if (e instanceof ClosedChannelException) {
                 LOGGER.info("Connection closed during protocol change");
diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 8562c9e80c222cada5ac460358181f3fd69be817..59cb0e471cc94d798db67156a5fe0093bab2e584 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -751,25 +751,28 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
     }
     // Paper end - optimise chunk tick iteration
 
-    public void collectSpawningChunks(final List<LevelChunk> output) {
+    // DivineMC start - Regionized Chunk Ticking
+    synchronized void collectSpawningChunks(final List<LevelChunk> output) {
         // Paper start - optimise chunk tick iteration
-        final ca.spottedleaf.moonrise.common.list.ReferenceList<net.minecraft.world.level.chunk.LevelChunk> tickingChunks = ((ca.spottedleaf.moonrise.patches.chunk_tick_iteration.ChunkTickServerLevel)this.level).moonrise$getPlayerTickingChunks();
+        final ca.spottedleaf.moonrise.common.list.ReferenceList<net.minecraft.world.level.chunk.LevelChunk> tickingChunks;
+        synchronized (tickingChunks = ((ca.spottedleaf.moonrise.patches.chunk_tick_iteration.ChunkTickServerLevel)this.level).moonrise$getPlayerTickingChunks()) {
+            final LevelChunk[] raw = tickingChunks.toArray(new LevelChunk[0]);
+            final int size = tickingChunks.size();
 
-        final LevelChunk[] raw = tickingChunks.getRawDataUnchecked();
-        final int size = tickingChunks.size();
+            Objects.checkFromToIndex(0, size, raw.length);
+            for (int i = 0; i < size; ++i) {
+                final LevelChunk levelChunk = raw[i];
 
-        Objects.checkFromToIndex(0, size, raw.length);
-        for (int i = 0; i < size; ++i) {
-            final LevelChunk levelChunk = raw[i];
+                if (!this.isChunkNearPlayer((ChunkMap) (Object) this, levelChunk.getPos(), levelChunk)) {
+                    continue;
+                }
 
-            if (!this.isChunkNearPlayer((ChunkMap)(Object)this, levelChunk.getPos(), levelChunk)) {
-                continue;
+                output.add(levelChunk);
             }
-
-            output.add(levelChunk);
         }
         // Paper end - optimise chunk tick iteration
     }
+    // DivineMC end - Regionized Chunk Ticking
 
     public void forEachBlockTickingChunk(final Consumer<LevelChunk> tickingChunkConsumer) {
         this.distanceManager.forEachEntityTickingChunk(chunkPos -> {
@@ -797,11 +800,15 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
     public boolean anyPlayerCloseEnoughTo(final BlockPos pos, final int maxDistance) {
         Vec3 target = new Vec3(pos);
 
-        for (ServerPlayer player : this.playerMap.getAllPlayers()) {
-            if (this.playerIsCloseEnoughTo(player, target, maxDistance)) {
-                return true;
+        // DivineMC start - Regionized Chunk Ticking - fix crash
+        synchronized (this.playerMap) {
+            for (ServerPlayer player : this.playerMap.getAllPlayers()) {
+                if (this.playerIsCloseEnoughTo(player, target, maxDistance)) {
+                    return true;
+                }
             }
         }
+        // DivineMC end - Regionized Chunk Ticking - fix crash
 
         return false;
     }
diff --git a/net/minecraft/server/level/ServerChunkCache.java b/net/minecraft/server/level/ServerChunkCache.java
index 070c5f3cb119f3b989d2430dc898a631d6c7924a..41ef18dd82a27bd6abf2303a36587cb2676b2c8c 100644
--- a/net/minecraft/server/level/ServerChunkCache.java
+++ b/net/minecraft/server/level/ServerChunkCache.java
@@ -55,10 +55,10 @@ import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
 public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moonrise.patches.chunk_system.world.ChunkSystemServerChunkCache { // Paper - rewrite chunk system
-    private static final Logger LOGGER = LogUtils.getLogger();
+    protected static final Logger LOGGER = LogUtils.getLogger(); // DivineMC - Regionized Chunk Ticking - private -> protected
     private final DistanceManager distanceManager;
-    private final ServerLevel level;
-    private final Thread mainThread;
+    protected final ServerLevel level; // DivineMC - Regionized Chunk Ticking - private -> protected
+    public final Thread mainThread;
     private final ThreadedLevelLightEngine lightEngine;
     public final ServerChunkCache.MainThreadExecutor mainThreadProcessor;
     public final ChunkMap chunkMap;
@@ -71,8 +71,10 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
     private final long[] lastChunkPos = new long[4];
     private final @Nullable ChunkStatus[] lastChunkStatus = new ChunkStatus[4];
     private final @Nullable ChunkAccess[] lastChunk = new ChunkAccess[4];
-    private final List<LevelChunk> spawningChunks = new ObjectArrayList<>();
-    private final Set<ChunkHolder> chunkHoldersToBroadcast = new ReferenceOpenHashSet<>();
+    // DivineMC start - Regionized Chunk Ticking
+    private final ObjectArrayList<LevelChunk> spawningChunks = new ObjectArrayList<>();
+    private final Set<ChunkHolder> chunkHoldersToBroadcast = java.util.Collections.synchronizedSet(new ReferenceOpenHashSet<>());
+    // DivineMC end - Regionized Chunk Ticking
     @VisibleForDebug
     private NaturalSpawner.@Nullable SpawnState lastSpawnState;
     // DivineMC start - Pufferfish: Optimize mob spawning
@@ -159,7 +161,7 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
     // Paper end - rewrite chunk system
     // Paper start - chunk tick iteration optimisations
     private final ca.spottedleaf.moonrise.common.util.SimpleThreadUnsafeRandom shuffleRandom = new ca.spottedleaf.moonrise.common.util.SimpleThreadUnsafeRandom(0L);
-    private void iterateTickingChunksFaster() {
+    protected void iterateTickingChunksFaster(final CompletableFuture<Void> spawns) { // DivineMC - Regionized Chunk Ticking - private -> protected
         final ServerLevel world = this.level;
         final int randomTickSpeed = world.getGameRules().get(GameRules.RANDOM_TICK_SPEED);
 
@@ -179,14 +181,16 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
 
             // call mid-tick tasks for chunk system
             if ((i & 7) == 0) {
-                // DivineMC start - Parallel world ticking
-                if (!org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableParallelWorldTicking) {
-                    ((ca.spottedleaf.moonrise.patches.chunk_system.server.ChunkSystemMinecraftServer) this.level.getServer()).moonrise$executeMidTickTasks();
-                    continue;
-                }
-                // DivineMC end - Parallel world ticking
+                ((ca.spottedleaf.moonrise.patches.chunk_system.server.ChunkSystemMinecraftServer)this.level.getServer()).moonrise$executeMidTickTasks();
+                continue;
             }
         }
+
+        // DivineMC start - Regionized Chunk Ticking
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncNaturalSpawn) {
+            spawns.join();
+        }
+        // DivineMC end - Regionized Chunk Ticking
     }
     // Paper end - chunk tick iteration optimisations
 
@@ -512,19 +516,27 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
         long time = this.level.getGameTime();
         long timeDiff = time - this.lastInhabitedUpdate;
         this.lastInhabitedUpdate = time;
-        if (!this.level.isDebug()) {
-            ProfilerFiller profiler = Profiler.get();
-            profiler.push("pollingChunks");
-            if (this.level.tickRateManager().runsNormally() && this.level.paperConfig().unsupportedSettings.ticking.chunks) { // Paper - option to disable ticking
-                profiler.push("tickingChunks");
-                this.tickChunks(profiler, timeDiff);
-                profiler.pop();
-            }
+        // DivineMC start - Regionized Chunk Ticking
+        if (this.level.isDebug()) {
+            return;
+        }
 
+        ProfilerFiller profiler = Profiler.get();
+        profiler.push("pollingChunks");
+
+        if (!this.level.tickRateManager().runsNormally()) { // DivineMC - when frozen only broadcast changed chunks and don't run async mob spawning
             this.broadcastChangedChunks(profiler);
             profiler.pop();
+            return;
         }
 
+        profiler.push("tickingChunks");
+        this.tickChunks(profiler, timeDiff);
+        profiler.pop();
+        this.broadcastChangedChunks(profiler);
+        profiler.pop();
+        // DivineMC end - Regionized Chunk Ticking
+
         // DivineMC start - Pufferfish: Optimize mob spawning
         if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableAsyncSpawning) {
             for (ServerPlayer player : this.level.players()) {
@@ -570,14 +582,18 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
     private void broadcastChangedChunks(final ProfilerFiller profiler) {
         profiler.push("broadcast");
 
-        for (ChunkHolder chunkHolder : this.chunkHoldersToBroadcast) {
-            LevelChunk chunk = chunkHolder.getChunkToSend(); // Paper - rewrite chunk system
-            if (chunk != null) {
-                chunkHolder.broadcastChanges(chunk);
+        // DivineMC start - Regionized Chunk Ticking
+        synchronized (chunkHoldersToBroadcast) {
+            for (ChunkHolder chunkHolder : this.chunkHoldersToBroadcast) {
+                LevelChunk chunk = chunkHolder.getChunkToSend(); // Paper - rewrite chunk system
+                if (chunk != null) {
+                    chunkHolder.broadcastChanges(chunk);
+                }
             }
-        }
 
-        this.chunkHoldersToBroadcast.clear();
+            this.chunkHoldersToBroadcast.clear();
+        }
+        // DivineMC end - Regionized Chunk Ticking
         profiler.pop();
     }
 
@@ -628,6 +644,28 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
             spawningCategories = List.of();
         }
 
+        // DivineMC start - Regionized Chunk Ticking
+        final CompletableFuture<Void> spawns;
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncNaturalSpawn) {
+            spawns = CompletableFuture.runAsync(() -> naturalSpawn(profiler, spawningCategories, timeDiff), org.bxteam.divinemc.async.rct.RegionizedChunkTicking.REGION_EXECUTOR);
+        } else {
+            naturalSpawn(profiler, spawningCategories, timeDiff);
+            spawns = CompletableFuture.completedFuture(null);
+        }
+
+        profiler.popPush("tickTickingChunks");
+        this.iterateTickingChunksFaster(spawns); // Paper - chunk tick iteration optimizations // DivineMC - Regionized Chunk Ticking
+        if (doMobSpawning) {
+            profiler.popPush("customSpawners");
+            this.level.tickCustomSpawners(this.spawnEnemies);
+        }
+
+        profiler.pop();
+        // DivineMC end - Regionized Chunk Ticking
+    }
+
+    // DivineMC start - Regionized Chunk Ticking
+    private void naturalSpawn(final ProfilerFiller profiler, final List<MobCategory> spawningCategories, final long timeDiff) {
         List<LevelChunk> spawningChunks = this.spawningChunks;
 
         try {
@@ -648,16 +686,12 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
         } finally {
             spawningChunks.clear();
         }
+    }
 
-        profiler.popPush("tickTickingChunks");
-        this.iterateTickingChunksFaster(); // Paper - chunk tick iteration optimizations
-        if (doMobSpawning) {
-            profiler.popPush("customSpawners");
-            this.level.tickCustomSpawners(this.spawnEnemies);
-        }
-
-        profiler.pop();
+    protected net.minecraft.world.level.entity.EntityTickList getEntityTickList() {
+       return level.entityTickList;
     }
+    // DivineMC end - Regionized Chunk Ticking
 
     private void tickSpawningChunk(
         final LevelChunk chunk, final long timeDiff, final List<MobCategory> spawningCategories, final NaturalSpawner.SpawnState spawnCookie
@@ -888,8 +922,15 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
                 return true;
             }
 
-            // Paper - rewrite chunk system
-            return super.pollTask() | ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel)ServerChunkCache.this.level).moonrise$getChunkTaskScheduler().executeMainThreadTask(); // Paper - rewrite chunk system
+            // DivineMC start - Regionized Chunk Ticking - "crash fix" start
+            boolean mainPolled = false;
+            try {
+                mainPolled = super.pollTask();
+            } catch (java.util.NoSuchElementException ex) {
+                // Queue was empty; ignore and continue to scheduler execution.
+            }
+            return mainPolled | ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel)ServerChunkCache.this.level).moonrise$getChunkTaskScheduler().executeMainThreadTask();
+            // DivineMC end - Regionized Chunk Ticking - "crash fix" start
         }
     }
 }
diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 252c5c1e6d753e12028ae1e0ba990104f3a6db68..25f7d0e684330bb445ae727344d20bbc45a0224e 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -208,7 +208,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
     private final LevelTicks<Block> blockTicks = new LevelTicks<>(this::isPositionTickingWithEntitiesLoaded);
     private final LevelTicks<Fluid> fluidTicks = new LevelTicks<>(this::isPositionTickingWithEntitiesLoaded);
     private final PathTypeCache pathTypesByPosCache = new PathTypeCache();
-    private final Set<Mob> navigatingMobs = new ObjectOpenHashSet<>();
+    private final Set<Mob> navigatingMobs = java.util.Collections.synchronizedSet(new ObjectOpenHashSet<>()); // DivineMC - Regionized Chunk Ticking
     private volatile boolean isUpdatingNavigations;
     protected final Raids raids;
     private final ObjectLinkedOpenHashSet<BlockEventData> blockEvents = new ObjectLinkedOpenHashSet<>();
@@ -540,75 +540,88 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
    }
     // Paper end - rewrite chunk system
     // Paper start - chunk tick iteration
+    // DivineMC start - RCT sync fix
+    private final Object lock = new Object();
     private final ca.spottedleaf.moonrise.common.list.ReferenceList<LevelChunk> playerTickingChunks = new ca.spottedleaf.moonrise.common.list.ReferenceList<>(EMPTY_LEVEL_CHUNKS);
     private final it.unimi.dsi.fastutil.longs.Long2IntOpenHashMap playerTickingRequests = new it.unimi.dsi.fastutil.longs.Long2IntOpenHashMap();
 
     @Override
-    public final ca.spottedleaf.moonrise.common.list.ReferenceList<LevelChunk> moonrise$getPlayerTickingChunks() {
-        return this.playerTickingChunks;
+    public synchronized final ca.spottedleaf.moonrise.common.list.ReferenceList<LevelChunk> moonrise$getPlayerTickingChunks() {
+        synchronized (lock) {
+            return this.playerTickingChunks;
+        }
     }
 
     @Override
-    public final void moonrise$markChunkForPlayerTicking(final LevelChunk chunk) {
-        final ChunkPos pos = chunk.getPos();
-        if (!this.playerTickingRequests.containsKey(ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(pos))) {
-            return;
-        }
+    public synchronized final void moonrise$markChunkForPlayerTicking(final LevelChunk chunk) {
+        synchronized (lock) {
+            final ChunkPos pos = chunk.getPos();
+            if (!this.playerTickingRequests.containsKey(ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(pos))) {
+                return;
+            }
 
-        this.playerTickingChunks.add(chunk);
+            this.playerTickingChunks.add(chunk);
+        }
     }
 
     @Override
-    public final void moonrise$removeChunkForPlayerTicking(final LevelChunk chunk) {
-        this.playerTickingChunks.remove(chunk);
+    public synchronized final void moonrise$removeChunkForPlayerTicking(final LevelChunk chunk) {
+        synchronized (lock) {
+            this.playerTickingChunks.remove(chunk);
+        }
     }
 
     @Override
-    public final void moonrise$addPlayerTickingRequest(final int chunkX, final int chunkZ) {
-        ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread((ServerLevel)(Object)this, chunkX, chunkZ, "Cannot add ticking request async");
+    public synchronized final void moonrise$addPlayerTickingRequest(final int chunkX, final int chunkZ) {
+        synchronized (lock) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread((ServerLevel) (Object) this, chunkX, chunkZ, "Cannot add ticking request async");
 
-        final long chunkKey = ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(chunkX, chunkZ);
+            final long chunkKey = ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(chunkX, chunkZ);
 
-        if (this.playerTickingRequests.addTo(chunkKey, 1) != 0) {
-            // already added
-            return;
-        }
+            if (this.playerTickingRequests.addTo(chunkKey, 1) != 0) {
+                // already added
+                return;
+            }
 
-        final ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder chunkHolder = ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel)(ServerLevel)(Object)this).moonrise$getChunkTaskScheduler()
-            .chunkHolderManager.getChunkHolder(chunkKey);
+            final ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder chunkHolder = ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel) (ServerLevel) (Object) this).moonrise$getChunkTaskScheduler()
+                .chunkHolderManager.getChunkHolder(chunkKey);
 
-        if (chunkHolder == null || !chunkHolder.isTickingReady()) {
-            return;
-        }
+            if (chunkHolder == null || !chunkHolder.isTickingReady()) {
+                return;
+            }
 
-        this.playerTickingChunks.add((LevelChunk)chunkHolder.getCurrentChunk());
+            this.playerTickingChunks.add((LevelChunk) chunkHolder.getCurrentChunk());
+        }
     }
 
     @Override
-    public final void moonrise$removePlayerTickingRequest(final int chunkX, final int chunkZ) {
-        ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread((ServerLevel)(Object)this, chunkX, chunkZ, "Cannot remove ticking request async");
+    public synchronized final void moonrise$removePlayerTickingRequest(final int chunkX, final int chunkZ) {
+        synchronized (lock) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread((ServerLevel) (Object) this, chunkX, chunkZ, "Cannot remove ticking request async");
 
-        final long chunkKey = ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(chunkX, chunkZ);
-        final int val = this.playerTickingRequests.addTo(chunkKey, -1);
+            final long chunkKey = ca.spottedleaf.moonrise.common.util.CoordinateUtils.getChunkKey(chunkX, chunkZ);
+            final int val = this.playerTickingRequests.addTo(chunkKey, -1);
 
-        if (val <= 0) {
-            throw new IllegalStateException("Negative counter");
-        }
+            if (val <= 0) {
+                throw new IllegalStateException("Negative counter");
+            }
 
-        if (val != 1) {
-            // still has at least one request
-            return;
-        }
+            if (val != 1) {
+                // still has at least one request
+                return;
+            }
 
-        final ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder chunkHolder = ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel)(ServerLevel)(Object)this).moonrise$getChunkTaskScheduler()
-            .chunkHolderManager.getChunkHolder(chunkKey);
+            final ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder chunkHolder = ((ca.spottedleaf.moonrise.patches.chunk_system.level.ChunkSystemServerLevel) (ServerLevel) (Object) this).moonrise$getChunkTaskScheduler()
+                .chunkHolderManager.getChunkHolder(chunkKey);
 
-        if (chunkHolder == null || !chunkHolder.isTickingReady()) {
-            return;
-        }
+            if (chunkHolder == null || !chunkHolder.isTickingReady()) {
+                return;
+            }
 
-        this.playerTickingChunks.remove((LevelChunk)chunkHolder.getCurrentChunk());
+            this.playerTickingChunks.remove((LevelChunk) chunkHolder.getCurrentChunk());
+        }
     }
+    // DivineMC end - RCT sync fix
     // Paper end - chunk tick iteration
 
     // DivineMC start - MSPT Tracking for each world
@@ -704,20 +717,39 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         boolean syncWrites = server.forceSynchronousWrites();
         DataFixer fixerUpper = server.getFixerUpper();
         // Paper - rewrite chunk system
-        this.chunkSource = new ServerChunkCache(
-            this,
-            levelStorage,
-            fixerUpper,
-            server.getStructureManager(),
-            executor,
-            generator,
-            this.spigotConfig.viewDistance, // Spigot
-            this.spigotConfig.simulationDistance, // Spigot
-            syncWrites,
-            null, // Paper - rewrite chunk system
-            () -> server.overworld().getDataStorage()
-            , savedDataStorage // Paper - initialize SavedDataStorage earlier
-        );
+        // DivineMC start - Regionized Chunk Ticking
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableRegionizedChunkTicking) {
+            this.chunkSource = new org.bxteam.divinemc.async.rct.RegionizedChunkTicking(
+                this,
+                levelStorage,
+                fixerUpper,
+                server.getStructureManager(),
+                executor,
+                generator,
+                this.spigotConfig.viewDistance, // Spigot
+                this.spigotConfig.simulationDistance, // Spigot
+                syncWrites,
+                null, // Paper - rewrite chunk system
+                () -> server.overworld().getDataStorage()
+                , savedDataStorage // Paper - initialize SavedDataStorage earlier
+            );
+        } else {
+            this.chunkSource = new ServerChunkCache(
+                this,
+                levelStorage,
+                fixerUpper,
+                server.getStructureManager(),
+                executor,
+                generator,
+                this.spigotConfig.viewDistance, // Spigot
+                this.spigotConfig.simulationDistance, // Spigot
+                syncWrites,
+                null, // Paper - rewrite chunk system
+                () -> server.overworld().getDataStorage()
+                , savedDataStorage // Paper - initialize SavedDataStorage earlier
+            );
+        }
+        // DivineMC end - Regionized Chunk Ticking
         this.chunkSource.getGeneratorState().ensureStructuresGenerated();
         this.portalForcer = new PortalForcer(this);
         if (this.canHaveWeather()) {
@@ -930,40 +962,56 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
                 profiler.pop();
             }
 
-            io.papermc.paper.entity.activation.ActivationRange.activateEntities(this); // Paper - EAR
-            this.entityTickList
-                .forEach(
-                    entity -> {
-                        entity.activatedPriorityReset = false; // DivineMC - Dynamic Activation of Brain
-                        if (!entity.isRemoved()) {
-                            if (!tickRateManager.isEntityFrozen(entity)) {
-                                profiler.push("checkDespawn");
-                                entity.checkDespawn();
-                                profiler.pop();
-                                if (true) { // Paper - rewrite chunk system
-                                    Entity vehicle = entity.getVehicle();
-                                    if (vehicle != null) {
-                                        if (!vehicle.isRemoved() && vehicle.hasPassenger(entity)) {
-                                            return;
+            // DivineMC start - Regionized Chunk Ticking
+            if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableRegionizedChunkTicking) {
+                // Regions and their block ticks are computed during the chunk-source tick; entities are ticked
+                // here, after runBlockEvents, to preserve the vanilla phase order (block events -> entities)
+                if (this.chunkSource instanceof org.bxteam.divinemc.async.rct.RegionizedChunkTicking rct) {
+                    rct.tickEntitiesParallel();
+                }
+                if (this.paperConfig().unsupportedSettings.ticking.blockEntities) { // Paper - option to disable ticking
+                    profiler.popPush("blockEntities");
+                    this.tickBlockEntities();
+                    profiler.pop();
+                } // Paper - option to disable ticking
+                this.spigotConfig.currentPrimedTnt = 0; // Spigot
+            } else {
+                io.papermc.paper.entity.activation.ActivationRange.activateEntities(this); // Paper - EAR
+                this.entityTickList
+                    .forEach(
+                        entity -> {
+                            entity.activatedPriorityReset = false; // DivineMC - Dynamic Activation of Brain
+                            if (!entity.isRemoved()) {
+                                if (!tickRateManager.isEntityFrozen(entity)) {
+                                    profiler.push("checkDespawn");
+                                    entity.checkDespawn();
+                                    profiler.pop();
+                                    if (true) { // Paper - rewrite chunk system
+                                        Entity vehicle = entity.getVehicle();
+                                        if (vehicle != null) {
+                                            if (!vehicle.isRemoved() && vehicle.hasPassenger(entity)) {
+                                                return;
+                                            }
+
+                                            entity.stopRiding();
                                         }
 
-                                        entity.stopRiding();
+                                        profiler.push("tick");
+                                        this.guardEntityTick(this::tickNonPassenger, entity);
+                                        profiler.pop();
                                     }
-
-                                    profiler.push("tick");
-                                    this.guardEntityTick(this::tickNonPassenger, entity);
-                                    profiler.pop();
                                 }
                             }
                         }
-                    }
-                );
-            if (this.paperConfig().unsupportedSettings.ticking.blockEntities) { // Paper - option to disable ticking
-            profiler.popPush("blockEntities");
-            this.tickBlockEntities();
-            profiler.pop();
-            } // Paper - option to disable ticking
-            this.spigotConfig.currentPrimedTnt = 0; // Spigot
+                    );
+                if (this.paperConfig().unsupportedSettings.ticking.blockEntities) { // Paper - option to disable ticking
+                    profiler.popPush("blockEntities");
+                    this.tickBlockEntities();
+                    profiler.pop();
+                } // Paper - option to disable ticking
+                this.spigotConfig.currentPrimedTnt = 0; // Spigot
+            }
+            // DivineMC end - Regionized Chunk Ticking
         }
 
         profiler.push("entityManagement");
@@ -1754,10 +1802,13 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         } else {
             if (entity instanceof net.minecraft.world.entity.item.ItemEntity item && item.getItem().isEmpty()) return false; // Paper - Prevent empty items from being added
             // Paper start - capture all item additions to the world
+            // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+            final java.util.List<net.minecraft.world.entity.item.ItemEntity> captureDrops = this.captureDrops.get();
             if (captureDrops != null && entity instanceof net.minecraft.world.entity.item.ItemEntity item) {
                 captureDrops.add(item);
                 return true;
             }
+            // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
             // Paper end - capture all item additions to the world
             // SPIGOT-6415: Don't call spawn event when reason is null. For example when an entity teleports to a new world.
             if (spawnReason != null && !org.bukkit.craftbukkit.event.CraftEventFactory.doEntityAddEventCalling(this, entity, spawnReason)) {
@@ -1960,7 +2011,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
     }
 
     @Override
-    public void sendBlockUpdated(final BlockPos pos, final BlockState old, final BlockState current, final @Block.UpdateFlags int updateFlags) {
+    public synchronized void sendBlockUpdated(final BlockPos pos, final BlockState old, final BlockState current, final @Block.UpdateFlags int updateFlags) { // DivineMC - Regionized Chunk Ticking - synchronized
         if (this.isUpdatingNavigations) {
             String message = "recursive call to sendBlockUpdated";
             Util.logAndPauseIfInIde("recursive call to sendBlockUpdated", new IllegalStateException("recursive call to sendBlockUpdated"));
@@ -1974,22 +2025,16 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         if (Shapes.joinIsNotEmpty(oldShape, newShape, BooleanOp.NOT_SAME)) {
             List<PathNavigation> navigationsToUpdate = new ObjectArrayList<>();
 
-            try { // Paper - catch CME see below why
-            for (Mob navigatingMob : this.navigatingMobs) {
-                PathNavigation pathNavigation = navigatingMob.getNavigation();
-                if (pathNavigation.shouldRecomputePath(pos)) {
-                    navigationsToUpdate.add(pathNavigation);
+            // DivineMC start - Regionized Chunk Ticking
+            synchronized (this.navigatingMobs) {
+                for (Mob navigatingMob : this.navigatingMobs) {
+                    PathNavigation pathNavigation = navigatingMob.getNavigation();
+                    if (pathNavigation.shouldRecomputePath(pos)) {
+                        navigationsToUpdate.add(pathNavigation);
+                    }
                 }
             }
-            // Paper start - catch CME see below why
-            } catch (final java.util.ConcurrentModificationException concurrentModificationException) {
-                // This can happen because the pathfinder update below may trigger a chunk load, which in turn may cause more navigators to register
-                // In this case we just run the update again across all the iterators as the chunk will then be loaded
-                // As this is a relative edge case it is much faster than copying navigators (on either read or write)
-                this.sendBlockUpdated(pos, old, current, updateFlags);
-                return;
-            }
-            // Paper end - catch CME see below why
+            // DivineMC end - Regionized Chunk Ticking
 
             try {
                 this.isUpdatingNavigations = true;
@@ -2007,17 +2052,17 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
     @Override
     public void updateNeighborsAt(final BlockPos pos, final Block sourceBlock) {
         // CraftBukkit start
-        if (this.populating) {
+        if (this.populating.get()) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             return;
         }
         // CraftBukkit end
-        if (captureBlockStates) { return; } // Paper - Cancel all physics during placement
+        if (captureBlockStates.get()) { return; } // Paper - Cancel all physics during placement // DivineMC - Regionized Chunk Ticking - thread-safe block capture
         this.updateNeighborsAt(pos, sourceBlock, ExperimentalRedstoneUtils.initialOrientation(this, null, null));
     }
 
     @Override
     public void updateNeighborsAt(final BlockPos pos, final Block sourceBlock, final @Nullable Orientation orientation) {
-        if (captureBlockStates) { return; } // Paper - Cancel all physics during placement
+        if (captureBlockStates.get()) { return; } // Paper - Cancel all physics during placement // DivineMC - Regionized Chunk Ticking - thread-safe block capture
         this.neighborUpdater.updateNeighborsAtExceptFromFacing(pos, sourceBlock, null, orientation);
     }
 
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index c967db4f3510cdab80d7410afbbeadfd3b81cf7b..f3aa83c6375dd42073ffbcb9414e45e9750c7967 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -445,6 +445,11 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     private boolean compassBar = false; // Purpur - Add compass command
     private boolean ramBar = false; // Purpur - Implement rambar commands
     public boolean hasTickedAtLeastOnceInNewWorld = false; // DivineMC - Parallel world ticking
+    // DivineMC start - Regionized Chunk Ticking
+    public final org.bxteam.divinemc.async.rct.RollingLongBuffer avgTickTimeNanos = new org.bxteam.divinemc.async.rct.RollingLongBuffer(100);
+    public int lastRegionChunkSize, lastRegionEntityAmount, regionHash;
+    public long regionBlockTickNanos;
+    // DivineMC end - Regionized Chunk Ticking
 
     // Paper start - rewrite chunk system
     private ca.spottedleaf.moonrise.patches.chunk_system.player.RegionizedPlayerChunkLoader.PlayerChunkLoaderData chunkLoader;
diff --git a/net/minecraft/server/level/ServerPlayerGameMode.java b/net/minecraft/server/level/ServerPlayerGameMode.java
index 74c45502c02043813d1f4e0b10f2ccceb34168c6..e6b644fcaf34199a0303e10b314eada894230284 100644
--- a/net/minecraft/server/level/ServerPlayerGameMode.java
+++ b/net/minecraft/server/level/ServerPlayerGameMode.java
@@ -398,7 +398,7 @@ public class ServerPlayerGameMode {
 
         // CraftBukkit start
         org.bukkit.block.BlockState bState = bblock.getState();
-        this.level.captureDrops = new java.util.ArrayList<>();
+        this.level.captureDrops.set(new java.util.ArrayList<>()); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
         // CraftBukkit end
         BlockState adjustedState = block.playerWillDestroy(this.level, pos, state, this.player);
         boolean changed = this.level.removeBlock(pos, false);
@@ -424,8 +424,10 @@ public class ServerPlayerGameMode {
         } // CraftBukkit
 
         // CraftBukkit start
-        List<net.minecraft.world.entity.item.ItemEntity> itemsToDrop = this.level.captureDrops; // Paper - capture all item additions to the world
-        this.level.captureDrops = null; // Paper - capture all item additions to the world; Remove this earlier so that we can actually drop stuff
+        // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+        java.util.List<net.minecraft.world.entity.item.ItemEntity> itemsToDrop = this.level.captureDrops.get(); // Paper - capture all item additions to the world
+        this.level.captureDrops.set(null); // Paper - capture all item additions to the world; Remove this earlier so that we can actually drop stuff
+        // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
         if (event.isDropItems()) {
             org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockDropItemEvent(bblock, bState, this.player, itemsToDrop); // Paper - capture all item additions to the world
         }
diff --git a/net/minecraft/world/item/ItemStack.java b/net/minecraft/world/item/ItemStack.java
index e163435f0e2d3a846330763e543fdc991507e5bb..c9f2155c4bb788f5e2304794e162d5322a287eee 100644
--- a/net/minecraft/world/item/ItemStack.java
+++ b/net/minecraft/world/item/ItemStack.java
@@ -383,8 +383,8 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
         boolean isBonemeal = usedItem == Items.BONE_MEAL;
 
         if (!(usedItem instanceof BucketItem)) {
-            level.captureBlockStates = true;
-            level.captureTreeGeneration = isBonemeal;
+            level.captureBlockStates.set(true); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+            level.captureTreeGeneration.set(isBonemeal); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
         }
 
         List<org.bukkit.craftbukkit.block.CraftBlockState> capturedBlockStates;
@@ -394,14 +394,16 @@ public final class ItemStack implements DataComponentHolder, ItemInstance {
         try {
             result = usedItem.useOn(context);
         } finally {
-            level.captureBlockStates = false;
-            level.captureTreeGeneration = false;
+            // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+            level.captureBlockStates.set(false);
+            level.captureTreeGeneration.set(false);
 
-            capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.values());
-            level.capturedBlockStates.clear();
+            capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.get().values());
+            level.capturedBlockStates.get().clear();
 
-            capturedBlockEntities = new java.util.ArrayList<>(level.capturedBlockEntities.values());
-            level.capturedBlockEntities.clear();
+            capturedBlockEntities = new java.util.ArrayList<>(level.capturedTileEntities.get().values());
+            level.capturedTileEntities.get().clear();
+            // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
 
             // DivineMC start - Parallel world ticking
             treeType = net.minecraft.world.level.block.SaplingBlock.getTreeTypeTL();
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index ca9bf3bcdb60bc99b815f3df219af11ae1177c77..18bcd275208cef257ef3621307707216d281b20c 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -121,7 +121,7 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         .build();
     public final it.unimi.dsi.fastutil.objects.ObjectArrayList<TickingBlockEntity> blockEntityTickers = new it.unimi.dsi.fastutil.objects.ObjectArrayList<>(); // DivineMC - compact block entity tickers in place - Fix MC-117075
     protected final CollectingNeighborUpdater neighborUpdater;
-    private final List<TickingBlockEntity> pendingBlockEntityTickers = Lists.newArrayList();
+    private final List<TickingBlockEntity> pendingBlockEntityTickers = java.util.Collections.synchronizedList(Lists.newArrayList()); // DivineMC - Regionized Chunk Ticking
     private boolean tickingBlockEntities;
     protected final Thread thread;
     private final boolean isDebug;
@@ -150,12 +150,13 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     private final CraftWorld world;
     public org.bukkit.generator.@Nullable ChunkGenerator generator;
 
-    public boolean captureBlockStates = false;
-    public boolean captureTreeGeneration = false;
-    public Map<BlockPos, org.bukkit.craftbukkit.block.CraftBlockState> capturedBlockStates = new java.util.LinkedHashMap<>(); // Paper
-    public Map<BlockPos, BlockEntity> capturedBlockEntities = new java.util.LinkedHashMap<>(); // Paper - Retain block place order when capturing blockstates
-    @Nullable
-    public List<net.minecraft.world.entity.item.ItemEntity> captureDrops;
+    // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+    public final ThreadLocal<Boolean> captureBlockStates = ThreadLocal.withInitial(() -> Boolean.FALSE);
+    public final ThreadLocal<Boolean> captureTreeGeneration = ThreadLocal.withInitial(() -> Boolean.FALSE);
+    public final ThreadLocal<Map<BlockPos, org.bukkit.craftbukkit.block.CraftBlockState>> capturedBlockStates = ThreadLocal.withInitial(java.util.LinkedHashMap::new); // Paper
+    public final ThreadLocal<Map<BlockPos, BlockEntity>> capturedTileEntities = ThreadLocal.withInitial(java.util.LinkedHashMap::new); // Paper - Retain block place order when capturing blockstates
+    public final ThreadLocal<@Nullable List<net.minecraft.world.entity.item.ItemEntity>> captureDrops = new ThreadLocal<>();
+    // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
     public final it.unimi.dsi.fastutil.objects.Object2LongOpenHashMap<SpawnCategory> ticksPerSpawnCategory = new it.unimi.dsi.fastutil.objects.Object2LongOpenHashMap<>();
     // Paper start - EAR 2
     public int wakeupInactiveRemainingAnimals;
@@ -163,7 +164,7 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     public int wakeupInactiveRemainingMonsters;
     public int wakeupInactiveRemainingVillagers;
     // Paper end - EAR 2
-    public boolean populating;
+    public final ThreadLocal<Boolean> populating = ThreadLocal.withInitial(() -> Boolean.FALSE); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
     public final org.spigotmc.SpigotWorldConfig spigotConfig; // Spigot
     // Paper start - add paper world config
     private final io.papermc.paper.configuration.WorldConfiguration paperConfig;
@@ -1048,8 +1049,8 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     @Nullable
     public final BlockState getBlockStateIfLoaded(BlockPos pos) {
         // CraftBukkit start - tree generation
-        if (this.captureTreeGeneration) {
-            CraftBlockState previous = this.capturedBlockStates.get(pos);
+        if (this.captureTreeGeneration.get()) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+            CraftBlockState previous = this.capturedBlockStates.get().get(pos); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             if (previous != null) {
                 return previous.getHandle();
             }
@@ -1113,20 +1114,23 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     public boolean setBlock(final BlockPos pos, final BlockState blockState, final @Block.UpdateFlags int updateFlags, final int updateLimit) {
         if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableParallelWorldTicking) ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread((ServerLevel)this, pos, "Updating block asynchronously"); // DivineMC - Parallel world ticking (additional concurrency issues logs)
         // CraftBukkit start - tree generation
-        if (this.captureTreeGeneration) {
+        // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+        if (this.captureTreeGeneration.get()) {
+            final Map<BlockPos, CraftBlockState> captured = this.capturedBlockStates.get();
             // Paper start - Protect Bedrock and End Portal/Frames from being destroyed
             BlockState type = getBlockState(pos);
             if (!type.isDestroyable()) return false;
             // Paper end - Protect Bedrock and End Portal/Frames from being destroyed
-            CraftBlockState blockstate = this.capturedBlockStates.get(pos);
+            CraftBlockState blockstate = captured.get(pos);
             if (blockstate == null) {
                 blockstate = org.bukkit.craftbukkit.block.CapturedBlockState.getTreeBlockState(this, pos, updateFlags);
-                this.capturedBlockStates.put(pos.immutable(), blockstate);
+                captured.put(pos.immutable(), blockstate);
             }
             blockstate.setBlock(blockState);
             blockstate.setFlags(updateFlags);
             return true;
         }
+        // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
         // CraftBukkit end
         if (!this.isInValidBounds(pos)) {
             return false;
@@ -1140,17 +1144,21 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         Block block = blockState.getBlock();
         // CraftBukkit start - capture blockstates
         boolean captured = false;
-        if (this.captureBlockStates) {
+        // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+        final boolean captureBlockStates = this.captureBlockStates.get();
+        final Map<BlockPos, CraftBlockState> capturedBlockStates = captureBlockStates ? this.capturedBlockStates.get() : null;
+        if (captureBlockStates) {
             final CraftBlockState snapshot;
-            if (!this.capturedBlockStates.containsKey(pos)) {
+            if (!capturedBlockStates.containsKey(pos)) {
                 snapshot = (CraftBlockState) org.bukkit.craftbukkit.block.CraftBlock.at(this, pos).getState(); // Paper - use CB getState to get a suitable snapshot
-                this.capturedBlockStates.put(pos.immutable(), snapshot);
+                capturedBlockStates.put(pos.immutable(), snapshot);
                 captured = true;
             } else {
-                snapshot = this.capturedBlockStates.get(pos);
+                snapshot = capturedBlockStates.get(pos);
             }
             snapshot.setFlags(updateFlags); // Paper - always set the flag of the most recent call to mitigate issues with multiple update at the same pos with different flags
         }
+        // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
         // CraftBukkit end - capture blockstates
         BlockState oldState = chunk.setBlockState(pos, blockState, updateFlags);
         // DivineMC start - Imanity: Raytrace AntiXraySDK integration
@@ -1162,8 +1170,8 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         this.chunkPacketBlockController.onBlockChange(this, pos, blockState, oldState, updateFlags, updateLimit); // Paper - Anti-Xray
         if (oldState == null) {
             // CraftBukkit start - remove blockstate if failed (or the same)
-            if (this.captureBlockStates && captured) {
-                this.capturedBlockStates.remove(pos);
+            if (captureBlockStates && captured) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+                capturedBlockStates.remove(pos); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             }
             // CraftBukkit end
             return false;
@@ -1201,7 +1209,7 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         */ // CraftBukkit
 
         // CraftBukkit start
-        if (!this.captureBlockStates) { // Don't notify clients or update physics while capturing blockstates
+        if (!captureBlockStates) { // Don't notify clients or update physics while capturing blockstates // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             // Modularize client and physic updates
             // Spigot start
             try {
@@ -1373,8 +1381,8 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     @Override
     public BlockState getBlockState(final BlockPos pos) {
         // CraftBukkit start - tree generation
-        if (this.captureTreeGeneration) {
-            CraftBlockState previous = this.capturedBlockStates.get(pos); // Paper
+        if (this.captureTreeGeneration.get()) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+            CraftBlockState previous = this.capturedBlockStates.get().get(pos); // Paper // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             if (previous != null) {
                 return previous.getHandle();
             }
@@ -1550,10 +1558,14 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
 
     public void tickBlockEntities() {
         this.tickingBlockEntities = true;
-        if (!this.pendingBlockEntityTickers.isEmpty()) {
-            this.blockEntityTickers.addAll(this.pendingBlockEntityTickers);
-            this.pendingBlockEntityTickers.clear();
+        // DivineMC start - Regionized Chunk Ticking - synchronization fix
+        synchronized (pendingBlockEntityTickers) {
+            if (!this.pendingBlockEntityTickers.isEmpty()) {
+                this.blockEntityTickers.addAll(this.pendingBlockEntityTickers);
+                this.pendingBlockEntityTickers.clear();
+            }
         }
+        // DivineMC end - Regionized Chunk Ticking - synchronization fix
 
         // Paper - Fix MC-117075 use removeAll - remove iterator in favour of indexed for loop, ensuring compile error if something uses iter incorrectly
         boolean tickBlockEntities = this.tickRateManager().runsNormally();
@@ -1766,10 +1778,13 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
     public @Nullable BlockEntity getBlockEntity(final BlockPos pos) {
         if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.enableParallelWorldTicking) ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThreadOrAsyncThread((ServerLevel) this, "Cannot read world asynchronously"); // DivineMC - Parallel world ticking
         // Paper start - Perf: Optimize capturedTileEntities lookup
+        // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
         net.minecraft.world.level.block.entity.BlockEntity blockEntity;
-        if (!this.capturedBlockEntities.isEmpty() && (blockEntity = this.capturedBlockEntities.get(pos)) != null) {
+        final Map<BlockPos, BlockEntity> capturedTileEntities = this.capturedTileEntities.get();
+        if (!capturedTileEntities.isEmpty() && (blockEntity = capturedTileEntities.get(pos)) != null) {
             return blockEntity;
         }
+        // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
         // Paper end - Perf: Optimize capturedTileEntities lookup
         if (!this.isInValidBounds(pos)) {
             return null;
@@ -1785,8 +1800,8 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         BlockPos pos = blockEntity.getBlockPos();
         if (this.isInValidBounds(pos)) {
             // CraftBukkit start
-            if (this.captureBlockStates) {
-                this.capturedBlockEntities.put(pos.immutable(), blockEntity);
+            if (this.captureBlockStates.get()) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+                this.capturedTileEntities.get().put(pos.immutable(), blockEntity); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
                 return;
             }
             // CraftBukkit end
diff --git a/net/minecraft/world/level/block/BedBlock.java b/net/minecraft/world/level/block/BedBlock.java
index 4fd5e46deac615bdb3744149772962b68daf72ee..126c4347c164c306e435aa69680adadd101a9070 100644
--- a/net/minecraft/world/level/block/BedBlock.java
+++ b/net/minecraft/world/level/block/BedBlock.java
@@ -310,8 +310,17 @@ public class BedBlock extends HorizontalDirectionalBlock {
     @Override
     public void setPlacedBy(final Level level, final BlockPos pos, final BlockState state, final @Nullable LivingEntity by, final ItemStack itemStack) {
         super.setPlacedBy(level, pos, state, by, itemStack);
-        BlockPos otherPos = pos.relative(state.getValue(FACING));
-        level.setBlockAndUpdate(otherPos, state.setValue(PART, BedPart.HEAD));
+        if (!level.isClientSide()) {
+            BlockPos otherPos = pos.relative(state.getValue(FACING));
+            level.setBlock(otherPos, state.setValue(PART, BedPart.HEAD), Block.UPDATE_ALL);
+            // CraftBukkit start - SPIGOT-7315: Don't updated if we capture block states
+            if (level.captureBlockStates.get()) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+                return;
+            }
+            // CraftBukkit end
+            level.updateNeighborsAt(pos, Blocks.AIR);
+            state.updateNeighbourShapes(level, pos, Block.UPDATE_ALL);
+        }
     }
 
     public DyeColor getColor() {
diff --git a/net/minecraft/world/level/block/Block.java b/net/minecraft/world/level/block/Block.java
index f1b446aa35185c2eaa987dfe645e2caf3ddad2f4..ed10a3ce8e23526c936c0bfcda7292af73fc2b9d 100644
--- a/net/minecraft/world/level/block/Block.java
+++ b/net/minecraft/world/level/block/Block.java
@@ -509,11 +509,14 @@ public class Block extends BlockBehaviour implements ItemLike {
             ItemEntity entity = entityFactory.get();
             entity.setDefaultPickUpDelay();
             // CraftBukkit start
-            if (level.captureDrops != null) {
-                level.captureDrops.add(entity);
+            // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+            final java.util.List<ItemEntity> captureDrops = level.captureDrops.get();
+            if (captureDrops != null) {
+                captureDrops.add(entity);
             } else {
                 level.addFreshEntity(entity);
             }
+            // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
             // CraftBukkit end
         }
     }
diff --git a/net/minecraft/world/level/block/SaplingBlock.java b/net/minecraft/world/level/block/SaplingBlock.java
index bf9b94eeed932504bbe3604111bbb576890f000c..63e397e54abc82b0e301b4ebd4d19cef1476517b 100644
--- a/net/minecraft/world/level/block/SaplingBlock.java
+++ b/net/minecraft/world/level/block/SaplingBlock.java
@@ -74,19 +74,20 @@ public class SaplingBlock extends VegetationBlock implements BonemealableBlock {
             level.setBlock(pos, state.cycle(STAGE), Block.UPDATE_NONE);
         } else {
             // CraftBukkit start
-            if (level.captureTreeGeneration) {
+            // DivineMC start - Regionized Chunk Ticking - thread-safe block capture
+            if (level.captureTreeGeneration.get()) {
                 this.treeGrower.growTree(level, level.getChunkSource().getGenerator(), pos, state, random);
             } else {
-                level.captureTreeGeneration = true;
+                level.captureTreeGeneration.set(true); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
                 java.util.List<org.bukkit.craftbukkit.block.CraftBlockState> capturedBlockStates;
                 org.bukkit.TreeType treeTypeLocal;
                 try {
                     this.treeGrower.growTree(level, level.getChunkSource().getGenerator(), pos, state, random);
                 } finally {
-                    level.captureTreeGeneration = false;
+                    level.captureTreeGeneration.set(false); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
 
-                    capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.values());
-                    level.capturedBlockStates.clear();
+                    capturedBlockStates = new java.util.ArrayList<>(level.capturedBlockStates.get().values()); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
+                    level.capturedBlockStates.get().clear(); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
 
                     // DivineMC start - Parallel world ticking
                     treeTypeLocal = SaplingBlock.getTreeTypeTL();
@@ -108,6 +109,7 @@ public class SaplingBlock extends VegetationBlock implements BonemealableBlock {
                     }
                 }
             }
+            // DivineMC end - Regionized Chunk Ticking - thread-safe block capture
             // CraftBukkit end
         }
     }
diff --git a/net/minecraft/world/level/block/WitherSkullBlock.java b/net/minecraft/world/level/block/WitherSkullBlock.java
index b5c9f0cdc830438cb9a966cee9d0504767e9f06d..b8affcbbd5a06f54d02b6fd4e299cf05fd7a194d 100644
--- a/net/minecraft/world/level/block/WitherSkullBlock.java
+++ b/net/minecraft/world/level/block/WitherSkullBlock.java
@@ -43,7 +43,7 @@ public class WitherSkullBlock extends SkullBlock {
     }
 
     public static void checkSpawn(final Level level, final BlockPos pos) {
-        if (!level.captureBlockStates && level.getBlockEntity(pos) instanceof SkullBlockEntity placedSkull) { // CraftBukkit
+        if (!level.captureBlockStates.get() && level.getBlockEntity(pos) instanceof SkullBlockEntity placedSkull) { // CraftBukkit // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             checkSpawn(level, pos, placedSkull);
         }
     }
diff --git a/net/minecraft/world/level/chunk/LevelChunk.java b/net/minecraft/world/level/chunk/LevelChunk.java
index 3236fb6ebfe287c35e5b70da40e16415bcd7b789..2b0a444c9cf5ee87b64a70e7a8d648b9cc4eb4ec 100644
--- a/net/minecraft/world/level/chunk/LevelChunk.java
+++ b/net/minecraft/world/level/chunk/LevelChunk.java
@@ -432,7 +432,7 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
             return null;
         }
 
-        if (!this.level.isClientSide() && (flags & Block.UPDATE_SKIP_ON_PLACE) == 0 && (!this.level.captureBlockStates || newBlock instanceof net.minecraft.world.level.block.BaseEntityBlock)) { // CraftBukkit - Don't place while processing the BlockPlaceEvent, unless it's a BlockContainer. Prevents blocks such as TNT from activating when cancelled.
+        if (!this.level.isClientSide() && (flags & Block.UPDATE_SKIP_ON_PLACE) == 0 && (!this.level.captureBlockStates.get() || newBlock instanceof net.minecraft.world.level.block.BaseEntityBlock)) { // CraftBukkit - Don't place while processing the BlockPlaceEvent, unless it's a BlockContainer. Prevents blocks such as TNT from activating when cancelled. // DivineMC - Regionized Chunk Ticking - thread-safe block capture
             state.onPlace(this.level, pos, oldState, movedByPiston);
         }
 
@@ -476,7 +476,7 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
 
     public @Nullable BlockEntity getBlockEntity(final BlockPos pos, final LevelChunk.EntityCreationType creationType) {
         // CraftBukkit start
-        BlockEntity blockEntity = this.level.capturedBlockEntities.get(pos);
+        BlockEntity blockEntity = this.level.capturedTileEntities.get().get(pos); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
         if (blockEntity == null) {
             blockEntity = this.blockEntities.get(pos);
         }
@@ -663,13 +663,13 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
 
                 org.bukkit.World world = this.level.getWorld();
                 if (world != null) {
-                    this.level.populating = true;
+                    this.level.populating.set(true); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
                     try {
                         for (org.bukkit.generator.BlockPopulator populator : world.getPopulators()) {
                             populator.populate(world, random, bukkitChunk);
                         }
                     } finally {
-                        this.level.populating = false;
+                        this.level.populating.set(false); // DivineMC - Regionized Chunk Ticking - thread-safe block capture
                     }
                 }
                 server.getPluginManager().callEvent(new org.bukkit.event.world.ChunkPopulateEvent(bukkitChunk));
diff --git a/net/minecraft/world/level/entity/EntityTickList.java b/net/minecraft/world/level/entity/EntityTickList.java
index 9f340f8ed56e1218f268e4f418dd6ddd87b1902a..a342ffd6b60744b65416baff2b1a46958b14397a 100644
--- a/net/minecraft/world/level/entity/EntityTickList.java
+++ b/net/minecraft/world/level/entity/EntityTickList.java
@@ -10,7 +10,7 @@ import net.minecraft.server.level.ServerLevel; // DivineMC - Parallel world tick
 import org.jspecify.annotations.Nullable;
 
 public class EntityTickList {
-    public final ca.spottedleaf.moonrise.common.list.IteratorSafeOrderedReferenceSet<net.minecraft.world.entity.Entity> entities = new ca.spottedleaf.moonrise.common.list.IteratorSafeOrderedReferenceSet<>(); // Paper - rewrite chunk system
+    public final ca.spottedleaf.moonrise.common.list.IteratorSafeOrderedReferenceSet<Entity> entities = new ca.spottedleaf.moonrise.common.list.IteratorSafeOrderedReferenceSet<>(Entity.class); // Paper - rewrite chunk system // DivineMC make our lives easier
 
     // DivineMC start - Parallel world ticking
     @Nullable
diff --git a/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java b/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java
index 1c7862ae4eaeb4d83e0fb423c86489f534e92be5..05a9b7e75d158c845f18f953ce00a95a3810c5e9 100644
--- a/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java
+++ b/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java
@@ -65,7 +65,7 @@ public class CollectingNeighborUpdater implements NeighborUpdater {
         this.addAndRun(pos, new CollectingNeighborUpdater.MultiNeighborUpdate(pos.immutable(), block, orientation, skipDirection));
     }
 
-    private void addAndRun(final BlockPos pos, final CollectingNeighborUpdater.NeighborUpdates update) {
+    private synchronized void addAndRun(final BlockPos pos, final CollectingNeighborUpdater.NeighborUpdates update) { // DivineMC - Regionized Chunk Ticking - synchronized
         boolean runningAlready = this.count > 0;
         boolean tooManyUpdates = this.maxChainedNeighborUpdates >= 0 && this.count >= this.maxChainedNeighborUpdates;
         this.count++;
@@ -84,7 +84,7 @@ public class CollectingNeighborUpdater implements NeighborUpdater {
         }
     }
 
-    private void runUpdates() {
+    private synchronized void runUpdates() { // DivineMC - Regionized Chunk Ticking - synchronized
         try {
             while (!this.stack.isEmpty() || !this.addedThisLayer.isEmpty()) {
                 for (int i = this.addedThisLayer.size() - 1; i >= 0; i--) {
