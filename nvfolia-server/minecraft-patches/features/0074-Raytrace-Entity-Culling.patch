From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: NONPLAYT <nonplay@bxteam.org>
Date: Sun, 19 Jul 2026 00:34:38 +0300
Subject: [PATCH] Raytrace Entity Culling

Server-side port of tr7zw's EntityCulling mod.
Entities that a player provably cannot see (fully hidden behind opaque blocks)
are removed from that player's entity tracker. This reduces network bandwidth,
improves client FPS and blinds entity-ESP cheats (e.g. freecam/x-ray style hacks
cannot see mobs, players or items through walls anymore).

diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 18c98b0aae4a941251ce1e0b140701ab8c84a843..325996aaaf2c566f8ecfc79b3c9933c88b6817fe 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -1493,6 +1493,7 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
                 && this.entity.broadcastToPlayer(player)
                 && ChunkMap.this.isChunkTracked(player, this.entity.chunkPosition().x(), this.entity.chunkPosition().z());
             // Paper end - Configurable entity tracking range by Y
+            visibleToPlayer = visibleToPlayer && !org.bxteam.divinemc.culling.EntityCullingManager.isCulled(player, this.entity); // DivineMC - Raytrace Entity Culling
             // CraftBukkit start - respect vanish API
             if (visibleToPlayer && !player.getBukkitEntity().canSeeChunkMapUpdatePlayer(this.entity.getBukkitEntity())) { // Paper - only consider hits // DivineMC - SparklyPaper: Optimize "canSee" checks
                 visibleToPlayer = false;
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index f3aa83c6375dd42073ffbcb9414e45e9750c7967..e9db74e8d676652574d99ad8556c05a383e8bb79 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -439,6 +439,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     public boolean suppressTrackerForLogin = false; // Paper - Fire PlayerJoinEvent when Player is actually ready
     // CraftBukkit end
     public boolean isRealPlayer; // Paper
+    public volatile org.bxteam.divinemc.culling.@Nullable PlayerCullTracker cullTracker; // DivineMC - Raytrace Entity Culling
     public com.destroystokyo.paper.event.entity.@Nullable PlayerNaturallySpawnCreaturesEvent playerNaturallySpawnedEvent; // Paper - PlayerNaturallySpawnCreaturesEvent
     public org.bukkit.event.player.PlayerQuitEvent.@Nullable QuitReason quitReason = null; // Paper - Add API for quit reason; there are a lot of changes to do if we change all methods leading to the event
     private boolean tpsBar = false; // Purpur - Implement TPSBar
@@ -824,6 +825,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
     public void doTick() {
         try {
+            org.bxteam.divinemc.culling.EntityCullingManager.tickPlayer(this); // DivineMC - Raytrace Entity Culling
             if (this.valid && (!this.isSpectator() || !this.touchingUnloadedChunk())) { // Paper - don't tick dead players that are not in the world currently (pending respawn)
                 super.tick();
                 if (!this.containerMenu.stillValid(this)) {
diff --git a/net/minecraft/world/entity/EntityType.java b/net/minecraft/world/entity/EntityType.java
index 1461bafd3f2bac631e63cd3e0b0ef97c575a9c0d..b649b087b05ec326e73a5eade14bb2805de961ee 100644
--- a/net/minecraft/world/entity/EntityType.java
+++ b/net/minecraft/world/entity/EntityType.java
@@ -67,6 +67,7 @@ public class EntityType<T extends Entity> implements EntityTypeTest<Entity, T>,
     private final int clientTrackingRange;
     private final int updateInterval;
     public boolean dabEnabled = false; // DivineMC - Dynamic Activation of Brain
+    public boolean raytraceCullingSkip = false; // DivineMC - Raytrace Entity Culling
     private final String descriptionId;
     private @Nullable Component description;
     private final Optional<ResourceKey<LootTable>> lootTable;
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index e39469d9876eee9ea571a8fc08bff3098a1b76dd..c1399dd75c8f6ff2e530ca7831fbfefeab98d4ea 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -1168,6 +1168,7 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         }
         // DivineMC end - Imanity: Raytrace AntiXraySDK integration
         this.chunkPacketBlockController.onBlockChange(this, pos, blockState, oldState, updateFlags, updateLimit); // Paper - Anti-Xray
+        org.bxteam.divinemc.culling.EntityCullingManager.onBlockChange(this, pos); // DivineMC - Raytrace Entity Culling
         if (oldState == null) {
             // CraftBukkit start - remove blockstate if failed (or the same)
             if (captureBlockStates && captured) { // DivineMC - Regionized Chunk Ticking - thread-safe block capture
diff --git a/net/minecraft/world/waypoints/WaypointTransmitter.java b/net/minecraft/world/waypoints/WaypointTransmitter.java
index 2866b750a28cc32c2913d3c1afee95f05956982c..7691a41a08263944ea19536f971ad77158cacbee 100644
--- a/net/minecraft/world/waypoints/WaypointTransmitter.java
+++ b/net/minecraft/world/waypoints/WaypointTransmitter.java
@@ -21,6 +21,7 @@ public interface WaypointTransmitter extends Waypoint {
 
     static boolean doesSourceIgnoreReceiver(final LivingEntity source, final ServerPlayer receiver) {
         if (!receiver.getBukkitEntity().canSee(source.getBukkitEntity())) return true; // Paper - ignore if entity is hidden from player
+        if (org.bxteam.divinemc.culling.EntityCullingManager.isCulled(receiver, source)) return true; // DivineMC - Raytrace Entity Culling - hide culled players from locator bar
         if (receiver.isSpectator()) {
             return false;
         } else if (!source.isSpectator() && !source.hasIndirectPassenger(receiver)) {
